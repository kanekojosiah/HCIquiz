<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>High-Fidelity Website — Product Landing</title>
  <style>
    :root{
      --bg:#f5f7fb;
      --card:#ffffff;
      --muted:#6b7280;
      --accent:#0f172a;
      --primary:#0ea5a4;
      --glass: rgba(255,255,255,0.6);
      --shadow: 0 8px 30px rgba(15,23,42,0.08);
      --radius:16px;
    }
    *{box-sizing:border-box}
    html,body{height:100%;margin:0;font-family:Inter, system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial; color:var(--accent);background:linear-gradient(180deg,#f7fbff 0%,var(--bg) 60%)}
    a{color:inherit;text-decoration:none}

    .container{max-width:1100px;margin:36px auto;padding:28px}
    header{display:flex;align-items:center;justify-content:space-between;gap:16px}

    /* Logo */
    .logo{display:flex;align-items:center;gap:10px}
    .logo-mark{width:48px;height:48px;border-radius:10px;background:linear-gradient(135deg,var(--primary),#06b6d4);display:flex;align-items:center;justify-content:center;color:white;font-weight:700;box-shadow:0 6px 20px rgba(14,165,164,0.18)}
    .logo-text{font-weight:700;font-size:18px}

    /* Nav */
    nav{display:flex;gap:14px;align-items:center}
    .nav-link{padding:8px 12px;border-radius:10px;color:var(--muted);font-weight:600}
    .nav-link.primary{background:transparent;color:var(--accent)}

    /* mobile toggle */
    .menu-btn{display:none;background:transparent;border:0;padding:8px;border-radius:8px}

    /* HERO */
    .hero{display:grid;grid-template-columns:1fr 420px;gap:28px;margin-top:22px;align-items:center}
    .hero-left{padding:18px}
    .eyebrow{display:inline-block;background:rgba(14,165,164,0.12);color:var(--primary);padding:6px 10px;border-radius:999px;font-weight:700;font-size:13px}
    .headline{font-size:38px;line-height:1.05;margin:16px 0;color:var(--accent)}
    .sub{color:var(--muted);font-size:16px;margin-bottom:18px}
    .cta-row{display:flex;gap:12px;align-items:center}
    .btn{padding:12px 18px;border-radius:12px;cursor:pointer;font-weight:700;border:0}
    .btn.primary{background:linear-gradient(90deg,var(--primary),#06b6d4);color:white;box-shadow:0 10px 30px rgba(6,182,212,0.14)}
    .btn.ghost{background:transparent;border:2px solid rgba(15,23,42,0.06);color:var(--accent)}

    .hero-right{background:linear-gradient(180deg,rgba(255,255,255,0.9),var(--glass));padding:18px;border-radius:16px;box-shadow:var(--shadow)}
    .hero-illustration{height:300px;border-radius:10px;background:linear-gradient(135deg,#eef8fb,#f5faff);display:flex;align-items:center;justify-content:center;border:1px solid rgba(12,18,22,0.03)}

    /* FEATURES */
    .features{display:grid;grid-template-columns:repeat(3,1fr);gap:16px;margin-top:26px}
    .feature{background:var(--card);padding:18px;border-radius:12px;box-shadow:0 6px 18px rgba(12,18,22,0.04);min-height:120px}
    .feature h4{margin:0 0 8px}
    .feature p{margin:0;color:var(--muted)}

    /* two columns section */
    .two-col{display:grid;grid-template-columns:1fr 420px;gap:18px;margin-top:20px;align-items:start}
    .card{background:var(--card);padding:18px;border-radius:12px;box-shadow:var(--shadow)}
    .chart-mock{height:220px;border-radius:10px;background:linear-gradient(180deg,#fff,#f7fbff);display:flex;align-items:center;justify-content:center;border:1px solid rgba(12,18,22,0.03)}

    /* form */
    form .field{display:flex;flex-direction:column;gap:8px;margin-bottom:12px}
    label{font-size:13px;color:var(--muted);font-weight:700}
    input[type="text"],input[type="email"],textarea{padding:12px;border-radius:10px;border:1px solid rgba(12,18,22,0.06);background:white;font-size:15px}
    textarea{min-height:120px}
    .form-hint{font-size:13px;color:var(--muted)}

    footer{margin-top:26px;text-align:center;color:var(--muted);font-size:14px}

    /* responsive */
    @media (max-width:980px){
      .hero{grid-template-columns:1fr}
      .two-col{grid-template-columns:1fr}
      .features{grid-template-columns:repeat(2,1fr)}
      nav{display:none}
      .menu-btn{display:inline-block}
    }
    @media (max-width:520px){
      .features{grid-template-columns:1fr}
      .logo-text{font-size:16px}
      .headline{font-size:26px}
    }
  </style>
</head>
<body>
  <div class="container">
    <header>
      <div class="logo" aria-label="Brand">
        <div class="logo-mark">P</div>
        <div class="logo-text">Prodify</div>
      </div>

      <nav aria-label="Primary navigation">
        <a class="nav-link" href="#features">Features</a>
        <a class="nav-link" href="#pricing">Pricing</a>
        <a class="nav-link" href="#contact">Contact</a>
        <a class="nav-link primary" href="#">Sign In</a>
      </nav>

      <button class="menu-btn" aria-label="Open menu" onclick="toggleMenu()">
        <svg width="26" height="26" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg"><path d="M4 7H20M4 12H20M4 17H20" stroke="#0f172a" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"/></svg>
      </button>
    </header>

    <main>
      <section class="hero" aria-labelledby="hero-title">
        <div class="hero-left">
          <div class="eyebrow">New • Launch</div>
          <h1 id="hero-title" class="headline">Design. Build. Delight — fast.</h1>
          <p class="sub">Ship beautiful interfaces with components that are accessible, responsive, and ready for production. Speed up your workflow without sacrificing craft.</p>

          <div class="cta-row">
            <button class="btn primary" onclick="document.getElementById('contact').scrollIntoView({behavior:'smooth'})">Get Started</button>
            <button class="btn ghost">Watch Demo</button>
          </div>

          <div style="margin-top:16px;color:var(--muted);font-size:13px">Trusted by teams at startups and enterprises — hundreds of monthly active users.</div>
        </div>

        <aside class="hero-right" aria-hidden="true">
          <div class="hero-illustration">
            <!-- simple SVG mockup -->
            <svg width="320" height="240" viewBox="0 0 320 240" fill="none" xmlns="http://www.w3.org/2000/svg">
              <rect x="8" y="8" width="304" height="224" rx="10" fill="#ffffff" stroke="#e6eef5"/>
              <rect x="24" y="26" width="120" height="14" rx="6" fill="#eef7f8"/>
              <rect x="24" y="52" width="272" height="10" rx="5" fill="#f3fbfc"/>
              <rect x="24" y="74" width="200" height="14" rx="6" fill="#e6f7f8"/>
              <rect x="24" y="100" width="180" height="80" rx="8" fill="#f8fdfe" stroke="#eef7f8"/>
            </svg>
          </div>
        </aside>
      </section>

      <section id="features" class="features" aria-label="Key features">
        <div class="feature">
          <h4>Intuitive Navigation</h4>
          <p>Clear structure and focused signposts help users move through flows without friction.</p>
        </div>
        <div class="feature">
          <h4>Smart Forms</h4>
          <p>Inline validation and clear labels reduce errors and speed up completion.</p>
        </div>
        <div class="feature">
          <h4>Actionable Data</h4>
          <p>Readable charts and tables that prioritize signal over noise for quick insights.</p>
        </div>
      </section>

      <section class="two-col">
        <div class="card">
          <div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:12px">
            <h3 style="margin:0">Dashboard snapshot</h3>
            <div style="color:var(--muted);font-size:13px">Updated 2 hours ago</div>
          </div>
          <div class="chart-mock">
            <!-- simple chart placeholder -->
            <svg width="420" height="180" viewBox="0 0 420 180" xmlns="http://www.w3.org/2000/svg">
              <rect width="420" height="180" rx="8" fill="#fff" stroke="#eef7fb"/>
              <polyline points="20,140 60,110 100,90 140,60 180,80 220,40 260,60 300,50 340,30 380,20" fill="none" stroke="url(#g)" stroke-width="4" stroke-linecap="round" stroke-linejoin="round"/>
              <defs><linearGradient id="g" x1="0" x2="1"><stop offset="0" stop-color="#06b6d4"/><stop offset="1" stop-color="#0ea5a4"/></linearGradient></defs>
            </svg>
          </div>

          <div style="display:flex;gap:12px;margin-top:12px;color:var(--muted);font-size:14px">
            <div style="flex:1">Conversion rate <strong style="color:var(--accent);">4.8%</strong></div>
            <div style="flex:1">Active users <strong style="color:var(--accent);">1,240</strong></div>
          </div>
        </div>

        <aside id="contact" class="card">
          <h3 style="margin-top:0">Contact us</h3>
          <p class="form-hint">Have questions? Send us a message and we'll get back within 1 business day.</p>
          <form onsubmit="return handleSubmit(event)" novalidate>
            <div class="field">
              <label for="name">Name</label>
              <input id="name" name="name" type="text" placeholder="Your full name" required />
            </div>
            <div class="field">
              <label for="email">Email</label>
              <input id="email" name="email" type="email" placeholder="you@example.com" required />
            </div>
            <div class="field">
              <label for="message">Message</label>
              <textarea id="message" name="message" placeholder="Tell us about your project" required></textarea>
            </div>

            <div style="display:flex;gap:10px;align-items:center">
              <button class="btn primary" type="submit">Send message</button>
              <div id="form-status" style="color:var(--muted);font-size:13px"></div>
            </div>
          </form>
        </aside>
      </section>

      <footer>
        <div>© <strong>Prodify</strong> — crafted with care. · <a href="#">Privacy</a> · <a href="#">Terms</a></div>
      </footer>
    </main>
  </div>

  <script>
    function toggleMenu(){
      alert('Mobile menu (demo) — in the full implementation this would open a drawer.');
    }

    function handleSubmit(e){
      e.preventDefault();
      const name = document.getElementById('name').value.trim();
      const email = document.getElementById('email').value.trim();
      const message = document.getElementById('message').value.trim();
      const status = document.getElementById('form-status');
      // simple validation
      if(!name || !email || !message){
        status.textContent = 'Please complete all fields.';
        status.style.color = '#ef4444';
        return false;
      }
      status.textContent = 'Sending...';
      status.style.color = '#6b7280';
      setTimeout(()=>{
        status.textContent = 'Thanks — we received your message.';
        status.style.color = '#059669';
        e.target.reset();
      },900);
      return false;
    }
  </script>
</body>
</html>

