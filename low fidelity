<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Low-Fidelity Website Wireframe</title>
  <style>
    /* Simple, neutral wireframe styling */
    :root{--bg:#f6f6f6;--panel:#e6e6e6;--muted:#9b9b9b;--accent:#d0d0d0}
    *{box-sizing:border-box}
    html,body{height:100%;margin:0;font-family:system-ui,-apple-system,Segoe UI,Roboto,"Helvetica Neue",Arial}
    body{background:var(--bg);color:#222;line-height:1.3}

    .wrap{max-width:1000px;margin:28px auto;padding:20px}
    header{display:flex;align-items:center;justify-content:space-between;padding:12px 16px;background:transparent}

    .logo{width:140px;height:40px;background:var(--panel);display:flex;align-items:center;justify-content:center;border:2px dashed #cfcfcf;color:var(--muted);font-weight:700}
    nav{display:flex;gap:12px}
    .nav-item{padding:8px 12px;background:var(--panel);border-radius:6px;border:2px dashed #d8d8d8;color:var(--muted)}

    .hero{display:grid;grid-template-columns:1fr 360px;gap:20px;margin-top:18px}
    .hero .copy{background:transparent}
    .headline{height:120px;background:var(--panel);border:3px dashed #ccc;padding:20px;font-size:28px;display:flex;flex-direction:column;justify-content:center}
    .sub{height:60px;background:var(--accent);border-radius:6px;margin-top:12px;padding:12px;color:var(--muted)}
    .cta{display:inline-block;margin-top:14px;padding:12px 18px;border-radius:8px;background:var(--panel);border:2px dashed #cfcfcf;color:var(--muted);font-weight:700}

    .image-placeholder{height:220px;background:linear-gradient(90deg,var(--panel),#ededed);border:3px dashed #ccc;display:flex;align-items:center;justify-content:center;color:var(--muted);font-weight:700}

    .features{display:grid;grid-template-columns:repeat(3,1fr);gap:14px;margin-top:18px}
    .feature{background:var(--panel);border:2px dashed #d0d0d0;padding:16px;min-height:110px}
    .feature h4{margin:0 0 8px 0;color:var(--muted)}

    .two-col{display:grid;grid-template-columns:1fr 1fr;gap:12px;margin-top:18px}
    .card{background:var(--panel);padding:14px;border:2px dashed #d0d0d0;min-height:140px}

    footer{margin-top:22px;padding:16px;text-align:center;color:var(--muted)}

    /* Make it reasonably responsive */
    @media (max-width:860px){
      .hero{grid-template-columns:1fr;}
      .features{grid-template-columns:repeat(2,1fr)}
      .wrap{padding:12px}
    }
    @media (max-width:520px){
      .features{grid-template-columns:1fr}
      nav{display:none}
    }

    /* Accessibility helpers for wireframe reviewers */
    .label{font-size:12px;color:var(--muted);margin-bottom:6px}
    .small-box{height:36px;background:var(--panel);border:2px dashed #d8d8d8;padding:8px;color:var(--muted);display:flex;align-items:center}
  </style>
</head>
<body>
  <div class="wrap">
    <header>
      <div class="logo">LOGO</div>
      <nav aria-label="Main navigation">
        <div class="nav-item">Nav 1</div>
        <div class="nav-item">Nav 2</div>
        <div class="nav-item">Nav 3</div>
      </nav>
    </header>

    <main>
      <!-- HERO -->
      <section class="hero" aria-labelledby="hero-title">
        <div class="copy">
          <div id="hero-title" class="headline">
            HERO TITLE — short, punchy message
          </div>
          <div class="sub">Short supporting line that explains the value proposition in 1–2 sentences.</div>
          <div class="cta" role="button" tabindex="0">Primary Action</div>
        </div>
        <aside class="image-placeholder" aria-hidden="true">IMAGE / ILLUSTRATION</aside>
      </section>

      <!-- FEATURES -->
      <section class="features" aria-label="Key features">
        <div class="feature">
          <div class="label">Feature 1</div>
          <div class="small-box">Icon</div>
          <p style="color:var(--muted);font-size:13px;margin-top:10px">Brief description placeholder — 1–2 lines.</p>
        </div>
        <div class="feature">
          <div class="label">Feature 2</div>
          <div class="small-box">Icon</div>
          <p style="color:var(--muted);font-size:13px;margin-top:10px">Brief description placeholder — 1–2 lines.</p>
        </div>
        <div class="feature">
          <div class="label">Feature 3</div>
          <div class="small-box">Icon</div>
          <p style="color:var(--muted);font-size:13px;margin-top:10px">Brief description placeholder — 1–2 lines.</p>
        </div>
      </section>

      <!-- TWO COLUMN CONTENT -->
      <section class="two-col">
        <div class="card">
          <div class="label">Content block</div>
          <div style="height:40px;background:#f0f0f0;border-radius:4px;margin-bottom:10px;display:flex;align-items:center;justify-content:center;color:var(--muted)">Media / Chart</div>
          <p style="color:var(--muted);font-size:13px">Place longer explanatory copy here. In a low-fidelity wireframe, keep content short and indicative.</p>
        </div>
        <div class="card">
          <div class="label">Contact / Form</div>
          <div style="display:flex;flex-direction:column;gap:8px;margin-top:8px">
            <div class="small-box">Name input</div>
            <div class="small-box">Email input</div>
            <div class="small-box" style="height:64px">Message textarea</div>
            <div class="cta" role="button" tabindex="0" style="width:fit-content">Send</div>
          </div>
        </div>
      </section>

    </main>

    <footer>
      Footer — small print, copyright, or links
    </footer>

    <!--
      NOTES FOR EDITING
      - Replace dashed borders with real styling when moving to high-fidelity.
      - Use <section> and <aside> as semantic placeholders for content grouping.
      - To export: save this file as .html and open in any browser.
      - Want a React + Tailwind version or a Figma-ready SVG wireframe? Ask and I will convert it.
    -->
  </div>
</body>
</html>

